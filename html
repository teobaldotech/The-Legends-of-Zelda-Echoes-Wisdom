<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Plano de Treino Gamer</title>

<style>
  :root {
    --cor-primaria: #2c3e50;
    --cor-secundaria: #3498db;
    --cor-hover: #2980b9;
    --cor-fundo: #f4f6f8;
    --cor-destaque: #f0f8ff;
    --borda: 8px;
  }

  body {
    font-family: 'Segoe UI', sans-serif;
    background: var(--cor-fundo);
    color: var(--cor-primaria);
    margin: 0;
    padding: 20px;
  }

  h1, h2 {
    color: var(--cor-primaria);
    margin-bottom: 10px;
  }

  button, select, input {
    background-color: var(--cor-secundaria);
    color: white;
    border: none;
    border-radius: var(--borda);
    padding: 10px 16px;
    margin: 6px 4px;
    cursor: pointer;
    font-size: 16px;
    transition: background 0.3s ease, box-shadow 0.3s ease;
  }

  input {
    background-color: white;
    color: var(--cor-primaria);
    border: 2px solid var(--cor-secundaria);
  }

  input:focus, select:focus {
    outline: none;
    box-shadow: 0 0 5px var(--cor-secundaria);
  }

  button:hover, select:hover {
    background-color: var(--cor-hover);
  }

  table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
    background-color: white;
    border-radius: var(--borda);
    overflow: hidden;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  }

  th, td {
    padding: 12px;
    text-align: left;
    border-bottom: 1px solid #ddd;
  }

  th {
    background-color: #ecf0f1;
  }

  tr {
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  tr.hide {
    opacity: 0;
    visibility: hidden;
    height: 0;
    overflow: hidden;
    display: none;
  }

  tr.show {
    display: table-row;
    opacity: 1;
    visibility: visible;
    animation: fadeSlide 0.4s ease forwards;
  }

  tr:hover {
    background-color: #ecf8ff;
  }

  @keyframes fadeSlide {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  #filtroStatus {
    font-style: italic;
    margin-top: 10px;
    color: #555;
  }

  @media (max-width: 600px) {
    button, select, input {
      width: 100%;
      margin: 8px 0;
    }

    table, th, td {
      font-size: 14px;
    }
  }
</style>
</head>
<body>

<h2>🎮 Plano de Treino Gamer</h2>

<section aria-label="Filtros para treinos">
  <label for="treinoFilter">Filtrar por tipo:</label>
  <select id="treinoFilter" aria-label="Filtrar por tipo de treino" onchange="filtrarTreinos()">
    <option value="todos">Todos</option>
    <option value="mira">Mira</option>
    <option value="estrategia">Estratégia</option>
    <option value="reflexos">Reflexos</option>
    <option value="coordenacao">Coordenação</option>
  </select>

  <input type="text" id="buscaTexto" placeholder="🔍 Buscar por nome ou descrição..." oninput="filtrarTreinos()" />
</section>

<p id="filtroStatus" aria-live="polite"></p>

<table id="treinoTable" aria-label="Tabela de treinos gamer">
  <caption class="sr-only">Tabela com treinos gamer, emojis e objetivos principais</caption>
  <thead>
    <tr>
      <th>Emoji</th>
      <th>Treino</th>
      <th>Descrição</th>
      <th>Objetivo</th>
      <th>Duração</th>
    </tr>
  </thead>
  <tbody id="treinoBody">
    <!-- Linhas geradas via JavaScript -->
  </tbody>
</table>

<script>
  const treinos = [
    { emoji: "🧘", nome: "Alongamento Rápido", desc: "Alongamento de mãos, punhos, ombros e pescoço", tipo: "reflexos", objetivo: "Preparação física", duracao: "5 min" },
    { emoji: "✊", nome: "Exercício de Dedos", desc: "Apertar bola de borracha ou digitar rápido", tipo: "coordenacao", objetivo: "Coordenação manual", duracao: "5 min" },
    { emoji: "🎯", nome: "Aquecimento de Mira", desc: "Prática de mira em alvos estáticos", tipo: "mira", objetivo: "Precisão", duracao: "5 min" },
    { emoji: "🏹", nome: "Tracking", desc: "Seguir inimigos em movimento", tipo: "mira", objetivo: "Rastreamento", duracao: "5-10 min" },
    { emoji: "⚡", nome: "Flick Shots", desc: "Treino de precisão rápida", tipo: "mira", objetivo: "Reação rápida", duracao: "5-10 min" },
    { emoji: "📺", nome: "Replay Analysis", desc: "Revisão de partidas e decisões", tipo: "estrategia", objetivo: "Estratégia", duracao: "15 min" },
    { emoji: "🗺️", nome: "Map Awareness", desc: "Estudo de mapas, posições e timings", tipo: "estrategia", objetivo: "Posicionamento", duracao: "15 min" },
    { emoji: "⏱️", nome: "Mini-jogos de Reflexo", desc: "Treinos online de reação rápida", tipo: "reflexos", objetivo: "Reflexo rápido", duracao: "10 min" },
    { emoji: "🎵", nome: "Jogos de Ritmo", desc: "Coordenação mão-olho com jogos de ritmo", tipo: "coordenacao", objetivo: "Coordenação", duracao: "15 min" },
    { emoji: "🎮", nome: "Partidas de Treino", desc: "Aplicar técnicas em partidas reais", tipo: "estrategia", objetivo: "Execução prática", duracao: "1-2 h" }
  ];

  function capitalizar(texto) {
    return texto.charAt(0).toUpperCase() + texto.slice(1);
  }

  function gerarTabela() {
    const corpoTabela = document.getElementById("treinoBody");
    corpoTabela.innerHTML = "";

    treinos.forEach(treino => {
      const tr = document.createElement("tr");
      tr.dataset.tipo = treino.tipo;
      tr.innerHTML = `
        <td>${treino.emoji}</td>
        <td>${treino.nome}</td>
        <td>${treino.desc}</td>
        <td>${treino.objetivo}</td>
        <td>${treino.duracao}</td>
      `;
      corpoTabela.appendChild(tr);
    });
  }

  function removerAcentos(str) {
    return str.normalize("NFD").replace(/[\u0300-\u036f]/g, "");
  }

  function filtrarTreinos() {
    const tipoSelecionado = document.getElementById('treinoFilter').value;
    const textoBusca = removerAcentos(document.getElementById('buscaTexto').value.toLowerCase());
    const linhas = document.querySelectorAll('#treinoTable tbody tr');

    let visiveis = 0;

    linhas.forEach(row => {
      const tipo = row.dataset.tipo;
      const nome = removerAcentos(row.children[1].textContent.toLowerCase());
      const desc = removerAcentos(row.children[2].textContent.toLowerCase());

      const correspondeTipo = (tipoSelecionado === 'todos' || tipo === tipoSelecionado);
      const correspondeTexto = (nome.includes(textoBusca) || desc.includes(textoBusca));

      if (correspondeTipo && correspondeTexto) {
        row.classList.remove('hide');
        row.classList.add('show');
        visiveis++;
      } else {
        row.classList.remove('show');
        row.classList.add('hide');
      }
    });

    let texto = textoBusca ? ` contendo "${textoBusca}"` : '';
    let tipoTexto = tipoSelecionado === 'todos' ? 'todos os treinos' : `treinos do tipo "${capitalizar(tipoSelecionado)}"`;

    document.getElementById('filtroStatus').textContent = visiveis
      ? `Mostrando ${visiveis} ${tipoTexto}${texto}`
      : "Nenhum treino encontrado.";
  }

  window.onload = () => {
    gerarTabela();
    filtrarTreinos();
  };
</script>

</body>
</html>